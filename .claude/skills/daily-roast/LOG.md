# Daily Roast 紀錄

> 每天一筆。新的放最上面。驗收欄位隔天回來補:✅ 做到 / ⚠️ 半套 / ❌ 沒動(+ 依據)。

## 範本

```
## YYYY-MM-DD

**昨天驗收**:①… ②… ③…

**① 專案/執行**
- 痛點:
- 證據:
- 今天做:
- 不做的代價:

**② 商業/成長**
- 痛點:
- 證據:
- 今天做:
- 不做的代價:

**③ 財務**
- 痛點:
- 證據:
- 今天做:
- 不做的代價:

**最低通關線**:第 X 條
```

---

## 2026-08-01

**昨天驗收**:第一天,無。

**① 專案/執行 —— 你不是在經營一個事業,你在收集半成品**
- 痛點:root 底下 12 個以上的作品資料夾,近兩個月真正有 commit 的只有 3 條線
  (health-quest-rpg、命理系列、skills)。其餘的不是「以後會做」,是**已經死了但沒發喪**,
  每天還在收你的注意力稅。
- 證據:`plantoflife` 只有一個 index.html;`IGagent`、`curve-app` 是空資料夾;
  `mind-map-ai`、`facialmonitor`、`skillmapstar`、`jr-game`、`line-translate-bot`
  近 2 個月 0 commit;root 散著 20 幾張 `test-*.png/jpeg`(自己的 CLAUDE.md 明文寫「工作檔不落在 root」)。
- 今天做(30 分鐘):在 `STATE.md` 第二節的表格加一欄「處置」,8 個閒置專案各寫一個字:
  **砍 / 凍 / 排**。寫「排」的必須同時寫一個日期,寫不出日期的一律算「凍」。
- 不做的代價:每個活著的半成品都在偷「我是不是該回去弄那個」的腦內頻寬。
  12 條線同時開著,等於每條線都拿到 1/12 的你——這是為什麼沒有一條走到能收錢。

**② 商業/成長 —— 你在調一台沒有燃料的引擎**
- 痛點:內容模型從 v1.0 一路升到 v2.2(reels 到 v2.5),但帳號還是 0 粉、baseline 個位數瀏覽。
  模型第七章 P3 是**你自己寫下的已驗證結論**:「養帳號優先於爆文,改文案救不了」。
  你讀了、寫了,然後繼續改文案。
- 證據:`threads-post/MODEL.md` 版本紀錄 7/6–7/28 共 12 次升級;同期唯一破千的 Case #7(1,134)
  是颱風時效 + 熱門標籤加持,模型自己註明「1,134 不是新常態」;Case #10 沒加持只有 9 瀏覽。
- 今天做(30 分鐘,不寫新貼文):去領域頭部帳號的近期爆文留言區,留 **3 則**
  10 字以上、講「我用 AI 做了什麼」的具體留言,各附一個能點的小作品
  (P5 轉粉 + P6 借流量,兩條都是你自己驗證過的機制)。留完把帳號/貼文記到 CASES 追蹤。
- 不做的代價:模型升到 v3.0,0 粉照樣 9 瀏覽。你會得到一份很漂亮的、沒有讀者的方法論。

**③ 財務 —— 你用範例數字在管一個家的錢**
- 痛點:`household-finance/data.json` 從 7/9 建立到今天還是**假數字**,檔案裡的 note 是你自己寫的
  「請換成你們的真實資料」。三週沒換,等於這個模型是做給空氣看的。
  拿假數字做決策,是最貴的一種節省。
- 證據:`meta.updated: 2026-07-09`、`meta.note` 明寫範例;`transactions` 只有 7 月初 4 筆,
  之後空白(發票同步機制寫好了,沒在跑)。
- 今天做(30 分鐘):把 4 塊真實數字填進去就好——`income`、`expenses`(固定支出)、
  `accounts`(現有現金)、`loans`(房貸餘額與利率)。**額外加一列**:
  把 AI 訂閱/網域/工具的月費,以「AI 事業支出」進 `expenses`——
  你會第一次看到這條 IP 每個月燒你多少錢。
- 不做的代價:目前整條漏斗的終點是**免費交付**,沒有任何一步在收錢。
  在你把成本算出來之前,你不會知道自己是在創業、還是在贊助陌生人學 AI。
  (投入若持續而營收為零,最壞情況是拿家庭現金流補事業支出——這一點務實面對,不用恐慌,但要看見。)

**最低通關線**:第 ③ 條。專案清一清明天也還在,貼文明天也能留,
但「用假數字管真錢」這件事每多一天都在複利。
