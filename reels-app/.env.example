# === 前端 ===
# 前端不用環境變數,直接編輯 config.js 填 SUPABASE_URL / SUPABASE_ANON_KEY

# === Edge Functions 秘密(用 supabase secrets set 設定,不要放前端)===
# supabase secrets set ANTHROPIC_API_KEY=sk-ant-...   # 腳本生成 + 影片歸因分析
# supabase secrets set OPENAI_API_KEY=sk-...          # Whisper 口播逐字稿(缺少時自動跳過逐字稿)
# supabase secrets set ANTHROPIC_MODEL=claude-sonnet-5  # 選填,預設 claude-sonnet-5
